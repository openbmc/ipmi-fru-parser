/* !!! WARNING: This is a GENERATED Code..Please do NOT Edit !!! */

#include <iostream>
#include <map>
#include <list>

using MetaDataDict = std::map<std::string,std::string>;
/* Dictionary of Dbus property and the associated metaData.*/
using PropertiesDict = std::map<std::string,MetaDataDict>;

/* Dictionary of Interface and the Properties. */
using InterfacePropertiesDict = std::map<std::string,PropertiesDict>;

/* Dictionary of object and the interfaces. */
using ObjectInterfaceDict = std::map<std::string,InterfacePropertiesDict>;

/* Dictionary of fru and the objects. */
using FruObjectDict = std::map<std::string,ObjectInterfaceDict>;

